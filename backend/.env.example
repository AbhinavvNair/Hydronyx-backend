# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017/
DATABASE_NAME=hydroai_db

# Authentication
SECRET_KEY=d79688a4930ab1250426d69af952b108de62ab080311bd1b53112d982b214ca3
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Server
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

# Email (SMTP) - optional; if not set, emails are logged to console
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@hydronyx.local

# Rate limiting (per-IP); increase if you hit 429 during development
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_PER_WINDOW=120

# Optional: Redis for rate limiting (if set, use Redis instead of in-memory)
REDIS_URL=

# Optional: Sentry for error logging
SENTRY_DSN=

# Optional: GIS constraint layers (paths to GeoJSON/raster)
LULC_LAYER_PATH=
PROTECTED_AREAS_PATH=
SOIL_PERMEABILITY_PATH=
DEM_SLOPE_PATH=
